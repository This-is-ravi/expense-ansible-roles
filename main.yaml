- name: "configure {{ component }} server"
  hosts: localhost
  connection: local
  vars_files:
  - "variable-{{ environment }}.yaml" #we r passing env, variables-dev.yaml , prod ..
  become: yes
  roles:
  - "{{ component }}"